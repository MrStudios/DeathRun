# Plugin Information
name: '${project.name}'
description: '${project.description}'
version: '${project.parent.version}'

# Dependencies
softdepend:
  - 'WorldEdit'

# Plugin Data
load: 'POSTWORLD'
api-version: '1.16'
main: 'pl.mrstudios.deathrun.plugin.Bootstrap'